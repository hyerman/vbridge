import { Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'action',
  template: '<span>Hi Hyer!</span>'
  //templateUrl: 'square.component.html',
})

export class ActionComponent {

}